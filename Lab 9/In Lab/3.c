#include <stdio.h>
#include <string.h>

int Shift(int arr[5],int n, int target)
{
    for (int i = 0; i < n; i++)
    {
        arr[0]==target;
        printf("%d",arr[i]);

    }
}
int main()
{
    int k;
    printf("enter the targeted value  less then 5: ");
    scanf("%d", &k);
    int arr[] = {1, 2, 3, 4, 5};
    int n = 5;
    Shift(arr,n, k);

    return 0;
}
